codecov:
  require_ci_to_pass: false

coverage:
  precision: 2
  # range: [90, 100]
  status:
    patch:
      default:
        target: 90%
        threshold: 1%
    project: false

comment:
  layout: 'header, diff, flags, files, footer'
